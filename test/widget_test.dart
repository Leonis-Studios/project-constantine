// Basic smoke test — verifies the app can be pumped without crashing.
//
// Full integration tests (buy/sell flow, price advance, etc.) belong in
// separate test files once the simulation is running.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — replace with real tests', () {
    expect(1 + 1, 2);
  });
}
